PROCEDURE Insertion_sort(VAR tab : ARRAY_OF INTERGER)
VAR
    i,n,j : INTERGER;
BEGIN
    n := tab.length;
    FOR n FROM 0 TO n-1 STEP 1  DO
    n := i ;
    END_FOR
    j:= arr[i];
        FOR j FROM 0 TO i-1 STEP 1 DO
            j := i+1 ;
            Insertion(j[i]);
        END_FOR
END